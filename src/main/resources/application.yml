spring:
  datasource:
    url: jdbc:mysql://${{MYSQLHOST}}:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username : root
    password : obNlpzpoYUiXECXjqooAimOWBJOYwbal
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:3f8a2b1c9d4e7f6a0b5c8d2e1f4a7b3c9d6e0f2a5b8c1d4e7f0a3b6c9d2e5f8}
  expiration: ${JWT_EXPIRATION:86400000}