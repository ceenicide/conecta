spring:
  datasource:
    url: ${MYSQL_URL} # Ou DATABASE_URL, veja no painel
    username: ${MYSQLUSER}
    password: ${MYSQLPASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update